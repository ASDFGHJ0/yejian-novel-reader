import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.yejian.reader",
  appName: "页间",
  webDir: "dist-mobile",
  android: {
    backgroundColor: "#f3efe4",
  },
};

export default config;
